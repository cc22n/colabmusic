from django.urls import path

from . import views

app_name = "audio"

urlpatterns = [
    # path("waveform/<int:track_id>/", views.waveform_data, name="waveform"),
    # path("status/<int:pk>/", views.processing_status, name="status"),
]
